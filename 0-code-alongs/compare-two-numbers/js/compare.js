
//Structure 
var left-side = document.querySelector(".first-number");
var right-side = document.querySelector(".second-number");
var middle = sign.text content; 

//Events
//which element, going to call what function 
compare.addEventListener("click", compare-numbers)

//Event handler functions

function compare-numbers(event)
	if  left-side > right-side , span = ">"
var middle = sign.text content 

		
		left-side = right-side, span = "="
var middle = sign.text content 
		
		left-side < right-side , span = "<"
var middle = sign.text content 

		if else 
				log 